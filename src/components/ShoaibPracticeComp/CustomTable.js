import React from 'react'
// import { TextForm } from '@platform/service-ui-libraries';
function CustomTable() {
  return (
    <div>
       {/* <TextForm
          name="advanced-filter-condition"
          className="advanced_text_field"
          multiline={true}
          rows={5}
          type="text"
          fieldValue={queryValue}
          variant={'filled'}
          placeholder={`Write a SQL query here\nE.g.: <column1_name> = <value> and <column2_name> = <value>`}
          onChange={setQueryValue}
          ref={(element) => {
            inputRefs.current[`advanced-query`] = element;
          }}
          validationsDetail={{
            validations: {
              required: true
            }
          }}
        /> */}
    </div>
  )
}

export default CustomTable
